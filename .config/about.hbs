THIRD-PARTY LICENSES

This binary includes code from the following third-party Rust crates:

{{#each licenses}}
{{#each used_by}}
{{#unless (eq crate.name "mockserver")}}
- {{crate.name}} {{crate.version}} ({{../id}})
{{/unless}}
{{/each}}
{{/each}}

================================================================================
FULL LICENSE TEXTS
================================================================================
{{#each licenses}}
{{#with (lookup used_by 0)}}
{{#unless (and (eq (len ../used_by) 1) (eq crate.name "mockserver"))}}

--------------------------------------------------------------------------------
{{../name}}{{#if ../id}} ({{../id}}){{/if}}
Used by: {{#each ../used_by}}{{#unless (eq crate.name "mockserver")}}{{crate.name}}{{#unless @last}}, {{/unless}}{{/unless}}{{/each}}
--------------------------------------------------------------------------------

{{{../text}}}
{{/unless}}
{{/with}}
{{/each}}
